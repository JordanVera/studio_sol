import {stories} from '@/lib/content';import Image from 'next/image';import Link from 'next/link';export const metadata={title:'Stories & flower care'};export default function Stories(){return <section className="section page-section"><p className="eyebrow">NOTES FROM THE STUDIO</p><h1>A little flower <em>wisdom.</em></h1><div className="stories-grid">{stories.map(s=><Link href={`/stories/${s.slug}`} key={s.slug}><Image src={s.image} width={800} height={600} alt={s.title}/><p className="eyebrow">{s.category}</p><h2>{s.title}</h2><p>{s.intro}</p><span className="text-link">Read the story →</span></Link>)}</div></section>}
