import {stories} from '@/lib/content';import {notFound} from 'next/navigation';import Image from 'next/image';export function generateStaticParams(){return stories.map(s=>({slug:s.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return {title:stories.find(s=>s.slug===slug)?.title||'Story'}}export default async function Story({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=stories.find(x=>x.slug===slug);if(!s)notFound();return <article className="article section"><p className="eyebrow">{s.category}</p><h1>{s.title}</h1><p>{s.intro}</p><Image src={s.image} width={1000} height={650} alt="Seasonal flowers arranged by hand"/>{s.body.map(p=><p key={p}>{p}</p>)}</article>}
